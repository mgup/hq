<div class="page-header">
  <h1>Выпускные квалификационные работы</h1>
</div>

<% @groups.sort_by(&:name).each do |group| %>
  <h2><%= group.name %></h2>

  <% group.students.for_vkr.each do |student| %>
    <h3><%= student.full_name %></h3>
  <% end %>
<% end %>
