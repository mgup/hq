<div class="page-header">
  <h1>Cписок университетов</h1>
  <h3>Все ВУЗы</h3>
</div>

<div class="row">
  <div class="col col-lg-10 col-offset-2">
    <%= link_to 'Новый университет', new_university_path, class: 'btn btn-primary' %>
    <%= link_to 'Экспорт в Excel', universities_path(format: 'xls'), class: 'btn btn-info' %>
    <%= link_to 'Вернуться к рецензиям', reviews_path, class: 'btn btn-primary' %>
  </div>
</div>

<table class="table table-condensed">
  <thead>
  <tr>
    <th>id</th>
    <th>Название</th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
  <%= render partial: 'rows' %>
  </tbody>
</table>

<div>
  <%= paginate_with_info @universities_page %>
</div>
