<%= render_navigation context: :student %>
<br>
<div class="page-header">
  <h1><%= @student.person.full_name %> <small><%= @student.group.name %></small></h1>
</div>

<p class="lead"><%= @student.person.hostel_status[:status] %></p>

<% unless @student.person.hostel_st == Person::HOSTEL_STATUS_NOT || @student.person.hostel_st == Person::HOSTEL_STATUS_EVICTSELF %>
<legend>Информация по общежитию</legend>
<div class="row">
  <div class="col-sm-4">
    <label>Общежитие</label>
  </div>
  <div class="col-sm-8">
    <p class="form-control-static"><%= @student.person.room.flat.hostel.short_name if @student.person.room%></p>
  </div>
</div>
<div class="row">
  <div class="col-sm-4">
    <label>Квартира</label>
   </div>
  <div class="col-sm-8">
    <p class="form-control-static"><%= @student.person.room.flat.number if @student.person.room %></p>
  </div>
</div>
<div class="row">
  <div class="col-sm-4">
    <label>Комната</label>
  </div>
  <div class="col-sm-8">
    <% if @student.person.room %>
      <p class="form-control-static"><%= @student.person.room.seats %>-х местная</p>
    <% end %>
  </div>
</div>

<legend>Информация по договору</legend>
<p>Надо подождать =(</p>

<legend>Информация по договору</legend>
<p>Надо подождать =(</p>

<% end %>